# Migration notes

A running log of every non-mechanical substitution made during the Python 2 → 3
port of `libwise` and `wise`. See `MIGRATION_PLAN.md` for the overall plan.

(Empty — populate as Phase 2+ proceeds.)
